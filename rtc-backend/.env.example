# Copy to .env and fill in real values. The .env file is gitignored.
# Per-app API keys/secrets live in the apps file (see apps.example.json).
WS_APPS_FILE=apps.json
# Shared secret other services send in the Authorization header to POST /publish.
WS_PUBLISH_SECRET=change-me-to-an-internal-secret
